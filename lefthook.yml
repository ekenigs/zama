# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: pnpm exec biome check --staged --files-ignore-unknown=true --no-errors-on-unmatched
    - name: typecheck
      run: pnpm exec tsc --noEmit
    - name: test
      run: pnpm test
